# Python Interpreter - CS 390 Final Project

# Python version required: 3.6 or higher

# Web interface requires Flask, core interpreter uses only standard library

# Web server dependency
flask==2.3.3

# To verify your Python version:
# python3 --version

# Core interpreter modules use only Python standard library:
# - enum (for TokenType enumeration)
# - sys (for command-line arguments)
# - unittest (for testing)
# - io (for output capture in web interface)
